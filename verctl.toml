# A bump leaves Cargo.lock stale, and ci.yml runs `cargo package --locked`.
[prepare]
after = ["cargo", "update", "--workspace"]
stage = ["Cargo.lock"]

[[packages]]
name = "ctl-core"
path = "Cargo.toml"
